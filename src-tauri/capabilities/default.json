{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the local launch screen.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
