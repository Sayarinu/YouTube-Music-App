[package]
name = "youtube-music-app"
version = "0.1.1"
description = "A lightweight desktop app for YouTube Music."
authors = ["Carter"]
edition = "2021"
license = "MIT"

[lib]
name = "youtube_music_mac_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
